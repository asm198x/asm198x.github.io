{
  "name": "asm198x.github.io",
  "type": "module",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "ui:fetch": "./scripts/fetch-ui.sh",
    "predev": "npm run ui:fetch && npm run docs:fetch",
    "dev": "astro dev",
    "prebuild": "npm run ui:fetch && npm run docs:fetch",
    "build": "astro build",
    "preview": "astro preview",
    "docs:fetch": "./scripts/fetch-asm198x.sh"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.3"
  }
}
